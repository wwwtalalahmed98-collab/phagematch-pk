# Local analysis environment — versions as actually run (Python 3.14.6, Windows 11).
# Pinned rather than range-specified so the archived release is reproducible.
pandas==3.0.5
numpy==2.5.2
matplotlib==3.11.1
openpyxl==3.1.5
python-docx==1.2.0

# Not required by the analysis itself:
#   - scipy is deliberately absent. The only test used is a two-sided Fisher
#     exact, implemented directly in scripts/14 and scripts/16 by summing the
#     hypergeometric tail, so the statistics carry no heavyweight dependency.
#
# Capsule typing did NOT run locally. It ran on Google Colab (Ubuntu, Python
# 3.12) with the versions below; see notebooks/ and README "Reproducing the
# typing step".
#   kleborate==3.2.4
#   kaptive==3.2.2      # pinned: 3.3.0 removed kaptive.database, which
#                       # Kleborate's capsule module imports
#   minimap2 2.24-r1122
#   ncbi-datasets-cli 18.35.0
#   mash 2.3
